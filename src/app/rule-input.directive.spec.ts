import { RuleInputDirective } from './rule-input.directive';

describe('RuleInputDirective', () => {
  it('should create an instance', () => {
    const directive = new RuleInputDirective();
    expect(directive).toBeTruthy();
  });
});
